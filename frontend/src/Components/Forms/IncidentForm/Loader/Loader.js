import React from "react";

function Loader({ message }) {
  const loaderStyle = {
    height: "20rem",
    textAlign: "center",
  };

  return <div class="ui centered inline loader"></div>;
}

export default Loader;
