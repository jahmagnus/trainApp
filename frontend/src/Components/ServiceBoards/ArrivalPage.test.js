import { screen, render } from "@testing-library/react";
import ArrivalPage from './Components/ServiceBoards/ArrivalPage'

test('when button clicked, stations are cleared fro value', () => {
    render(<ArrivalPage/>)
})